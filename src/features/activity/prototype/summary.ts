/**
 * PROTOTYPE — the one-line row grammar graduated into the shared read model
 * at fold-in (#147). Re-exported here so the archived variants keep running.
 */

export { rowHandle, rowSummary } from "../feed";
